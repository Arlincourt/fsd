

mixin advantage(params)
  -
    const text = params.text 
    const name = params.name 
    const iconClass = params.class
    const isBorder = params.border || undefined
    const bodyClasses = ['advantage__body']
    if(isBorder !== undefined) {
      bodyClasses.push('advantage__body_with_border')
    }
  .advantage
    div(class=bodyClasses)
      i(class="advantage__icon " + iconClass)
      .advantage__info 
        p.advantage__name= name 
        p.advantage__text= text